@model Product
@{
    ViewBag.Title = $"{Model.ProductName} ürün detayı";
}

<div class="row">
    <div class="col-sm-12">
        @using (Html.BeginForm("Update", "Product", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            @Html.HiddenFor(x=>x.ProductID)
            <div class="form-group">
                @Html.LabelFor(x => x.ProductName, new { @class = "col-sm-2 control-label" })
                <div class="col-sm-10">
                    @Html.TextBoxFor(x => x.ProductName, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.UnitPrice, new { @class = "col-sm-2 control-label" })
                <div class="col-sm-10">
                    @Html.TextBoxFor(x => x.UnitPrice, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.Discontinued, new { @class = "col-sm-2 control-label" })
                <div class="col-sm-10">
                    @Html.CheckBoxFor(x => x.Discontinued)
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn btn-warning">Update</button>
                </div>
            </div>
        }
    </div>
</div>

